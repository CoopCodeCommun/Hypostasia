# Admin desactive — l'interface d'administration n'est plus utilisee.
# Toute la gestion des analyseurs se fait via l'interface front HTMX (/api/analyseurs/).
# / Admin disabled — the admin interface is no longer used.
# All analyzer management is done via the front HTMX interface (/api/analyseurs/).
