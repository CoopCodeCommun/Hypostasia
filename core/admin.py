# Admin desactive — l'interface d'administration n'est plus utilisee.
# Toute la configuration se fait via l'interface front HTMX.
# / Admin disabled — the admin interface is no longer used.
# All configuration is done via the front HTMX interface.
